export interface OrderItem {
	roomTypeId: number;
	checkinDate: string;
	checkoutDate: string;
}

export interface Client {
	name: string;
	phoneNumber: string;
}

export interface Subscriber {
	name: string;
	phoneNumber: string;
}
